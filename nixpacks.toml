[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["rm -rf node_modules", "npm install"]

[phases.build]
cmds = ["echo '=== Building Attendance App v2.0.0 ==='", "echo '=== With Professional UI and Animations ==='"]

[start]
cmd = "bash railway-start.sh"
