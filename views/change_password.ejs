<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Change Password - Attendance</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="container">
    <div class="app-header">
      <h1>📚 ATTENDANCE APP BY PALLAV PRADHAN</h1>
    </div>

    <div class="login-card">
      <div class="login-header">
        <div class="login-icon">🔐</div>
        <h2>Change Password</h2>
        <p style="color: #888; font-size: 14px;">Update your account password</p>
      </div>

      <% if (typeof message !== 'undefined' && message) { %>
        <p class="info" style="animation: fadeIn 0.5s ease-out;"><%= message %></p>
      <% } %>
      <% if (typeof error !== 'undefined' && error) { %>
        <p class="error" style="animation: fadeIn 0.5s ease-out;"><%= error %></p>
      <% } %>

      <form method="post" action="/change-password">
        <div class="form-group">
          <label for="currentPassword">Current Password</label>
          <input
            type="password"
            id="currentPassword"
            name="currentPassword"
            class="form-input"
            placeholder="Enter current password"
            required
            autofocus
          />
        </div>

        <div class="form-group">
          <label for="newPassword">New Password</label>
          <input
            type="password"
            id="newPassword"
            name="newPassword"
            class="form-input"
            placeholder="Enter new password"
            required
          />
        </div>

        <div class="form-group">
          <label for="confirmPassword">Confirm New Password</label>
          <input
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            class="form-input"
            placeholder="Confirm new password"
            required
          />
        </div>

        <button type="submit" class="login-btn">
          <span style="position: relative; z-index: 1;">Update Password</span>
        </button>
      </form>

      <div class="forgot-password">
        <a href="<%= user && user.role === 'admin' ? '/admin' : user && user.role === 'teacher' ? '/teacher' : '/student' %>">Back to Dashboard</a>
      </div>
    </div>

    <div class="app-footer">
      🔒 IT IS ONLY FOR PERSONAL USE OF TEACHER PALLAVI PRADHAN
    </div>
  </div>
</body>
</html>
